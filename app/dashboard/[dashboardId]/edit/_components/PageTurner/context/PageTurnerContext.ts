import { createContext } from 'react';

// export interface PageTurnerContextProps {
//   currentPage: number;
//   setCurrentPage: (page: number) => void;
//   totalPage: number;
// }

// export const PageTurnerContext = createContext<PageTurnerContextProps | undefined>(undefined);

/**
 * TODO: api 연결 후 로직 구성
 */
export const PageTurnerContext = createContext<undefined>(undefined);
